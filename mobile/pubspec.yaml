name: lifeos
description: "مال تو (maaleto) — Persian life-management app: Finance, Tasks, Habits, Calendar, Notifications, Reports, all on the same LifeOS backend as the web client."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  dio: ^5.10.0
  flutter_riverpod: ^3.3.2
  go_router: ^17.3.0
  intl: ^0.20.3
  # Android-only in practice: TokenStore (lib/src/auth/token_store.dart)
  # still branches on Platform.isAndroid and keeps InMemoryTokenStore for the
  # plugin-free Windows dev build (its secure-storage backend needs Developer
  # Mode / symlink perms enabled on Windows, which desktop is not gating on).
  flutter_secure_storage: ^10.0.0-beta.4
  # Display-only Jalali conversion for the mobile UI (streaks, budgets,
  # calendar) — all boundary math stays server-side (Rule 1); this package
  # only formats a DateTime the server already computed. Mirrors the web's
  # jalaali-js usage in packages/core/src/shared/jalali.ts.
  shamsi_date: ^1.1.1
  # Client-generated Idempotency-Key on every Finance mutation (a fresh
  # uuid per submit) — matching CLAUDE.md's sync-ready convention so a
  # retried tap never double-posts a transaction/budget.
  uuid: ^4.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  # Generates every Android mipmap density + adaptive-icon layer from one
  # source PNG (assets/icon/icon.png — the landing page's girih star motif
  # on brand lapis) so debug/release builds stop shipping Flutter's
  # default icon. Re-run `dart run flutter_launcher_icons` after changing
  # the source image.
  flutter_launcher_icons: ^0.14.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/icon/icon.png
    - assets/icon/icon_foreground.png

  # Same 4 static weights the web bundles (apps/web/src/lib/fonts.ts), copied
  # from the vazirmatn npm package's ttf/ output — self-hosted for the same
  # reason as web: no network fetch at build time on this dev machine.
  fonts:
    - family: Vazirmatn
      fonts:
        - asset: assets/fonts/vazirmatn/Vazirmatn-Regular.ttf
          weight: 400
        - asset: assets/fonts/vazirmatn/Vazirmatn-Medium.ttf
          weight: 500
        - asset: assets/fonts/vazirmatn/Vazirmatn-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/vazirmatn/Vazirmatn-Bold.ttf
          weight: 700

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_launcher_icons:
  android: true
  image_path: "assets/icon/icon.png"
  # Adaptive icon: a transparent-background foreground (the star sized to
  # Android's ~66% center safe zone) over a plain lapis background layer,
  # so OEM launchers can crop/mask into a circle/squircle/rounded-square
  # without ever clipping the star itself.
  adaptive_icon_background: "#1E4798"
  adaptive_icon_foreground: "assets/icon/icon_foreground.png"
